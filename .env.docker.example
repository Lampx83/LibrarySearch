# Cổng frontend (giao diện web). Mặc định 8019.
HTTP_PORT=8019

# Cổng backend (API). AI Portal dùng URL http://<host>:BACKEND_PORT để gọi API. Mặc định 8020.
BACKEND_PORT=8020

# Đường dẫn thư mục dữ liệu *trong container* (backend mount volume tại /data).
LIBRARY_DATA_DIR=/data
